This is going to help us hold scripts such as validation file, so that we can just one it once and
it will validate out setup.

- `chmod +x scripts/validate.sh` - Give permission to the script -`./validate.sh dev` or `./validate.sh prod` - Run the validation file

We also create the deploy file to hold all the deployment command and we just one command we can just run the deployment and deploy to product.
